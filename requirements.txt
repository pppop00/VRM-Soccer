opencv-python>=4.5.0
numpy>=1.20.0
pandas>=1.3.0

# Scorer dependencies (scorer.py)
scikit-image>=0.19.0
torch>=2.0.0
torchvision>=0.15.0
open-clip-torch>=2.20.0
torchmetrics[multimodal]>=1.0.0

# Optional heavy scorers (install separately if needed):
# pip install dover
# pip install fvmd
